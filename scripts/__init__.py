"""Operator scripts: database bootstrap and verification.

Every script in here is runnable directly from the repository root, e.g.
`python scripts/bootstrap_db.py --sample`. This file exists so the directory
can also be imported (`from scripts import verify_db`) by tests.
"""
